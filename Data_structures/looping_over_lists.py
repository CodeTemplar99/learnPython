letters = ["a", "b", "c", "d"]
for index, letter in enumerate(letters):
    print(index, letter)

# this for loop loops over the letters list and returns a topple
# with the index and the value(letter)
#  the index variable and letter variable is passed to the for loop
# with the enumerate function.
# this then prints as a value
